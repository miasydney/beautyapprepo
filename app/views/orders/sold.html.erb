<h1>MANAGE YOUR LISTINGS: </h1>
<a class="btn btn-primary" href="/listings/new">Create New Listing</a>
<br>
<h2>Your Booked Listings:</h2>
<p>Congratulations! The following listings have been booked:</p>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th>Business</th>
      <th>Instagram</th>
      <th>Booked</th>
      <th>Date</th>
      <th>Time</th>
      <th>Seller</th>
      <th>Category</th>
      <th>City</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <%# note to self: style listings into cards %>
  <tbody>
  <%# @booked_appointments instance variable has been defined in orders_controller bought method %>
    <% @sold_appointments.each do |appointment| %>
      <tr>
        <td><%= image_tag appointment.listing.picture, class:"card-img-top" if appointment.listing.picture.attached? %></td>
        <td><%= appointment.listing.title %></td>
        <td><%= appointment.listing.description %></td>
        <td><%= number_to_currency(appointment.listing.price) %></td>
        <td><%= appointment.listing.business %></td>
        <td><%= appointment.listing.instagram %></td>
        <td><%= appointment.listing.date %></td>
        <td><%= (appointment.listing.time).strftime("%H:%M %p") %></td>
        <td><%= appointment.seller.firstname %></td>
        <td><%= appointment.listing.category.name %></td>
        <td><%= appointment.listing.city.name %></td>

        <td><%= appointment.listing.category.name %></td>
        <td><%= appointment.listing.city.name %></td>
        Contact details for <%= appointment.buyer.firstname %>
        <td><%= appointment.buyer.email %></td>
      </tr>
    <% end %>
  </tbody>
</table>
